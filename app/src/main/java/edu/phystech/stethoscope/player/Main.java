package edu.phystech.stethoscope.player;

public class Main
{
  public static void main(String[] args)
  {
    //Butterworth.initialize(Math.PI*0.25, Math.PI*0.6);
    
    //for (int i=0; i<n; i++)
    //  y[i] = Butterworth.process(x[i]);
    
    //Butterworth.refresh();
  }
}

